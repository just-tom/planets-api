[
{% for item in collection %}
    {
{% for key, element in item %}
    {% if not loop.last %}
    "{{ key }}": "{{ element }}",
    {% else %}
"{{ key }}": "{{ element }}"
    {% endif %}
{% endfor %}
{% if not loop.last %}
},
{% else %}
}
{% endif %}
{% endfor %}
]